# Oncology Aid — npm 配置
# 确保跨平台 lockfile 兼容（Windows / macOS / Linux）

# 统一使用 lockfile v3 格式（npm 7+，跨平台一致性强于 v1/v2）
lockfile-version=3

# 跨平台 CI 说明：
#   - 优先使用 npm install（自动适配当前平台）
#   - 若需 npm ci，先在目标平台执行 npm install --lockfile-version=3 生成 lockfile
#   - fsevents (macOS only) 在 Linux/Windows 上标记为 optional，不影响安装
